# Supabase → Project Settings → API
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...   # server-only — never expose this in client code

# Your deployed domain (used to build shareable links); use http://localhost:3000 for local dev
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
